import React from 'react'
import {
    View,
    Text,
    Image
} from 'react-native'

import {
    IconButton,
    TextButton
} from "../../components"

import {
    COLORS,
    SIZES,
    FONTS,
    icons,
    constants,
    images
} from "../../constants"

const Login = ({ }) => {
    return (
        <View
            style={{
                flex: 1,
                backgroundColor: 'red'
            }}
        >

        </View>
    )
}

export default Login;